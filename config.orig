# To which SPI device is the LED strip connected? (default: /dev/spidev2.0):
spidevice = /dev/spidev2.0

# How many RGB LED are connected? (default: 52):
#   This is probably twice the number of LPD8806 chips.
numleds = 52

# Delay between each image row (in seconds) (default: 0.03):
columndelay = 0.03

# Delay between repeats (in seconds) (defaut: 1.0):
repeatdelay = 1.0
